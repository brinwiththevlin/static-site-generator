from setuptools import setup, find_packages

setup(
    name="static-site-generator",
    version="0.0.1",  # Early version for now
    description="A simple static site generator in Python",
    packages=find_packages(),
    python_requires=">=3.7",
)
